name: dynamic-include
priority: 10
sources:
  - id: transforms
    type: file
    path: tests/fixtures/dynamic-pipelines/sources/transforms.json
    format: json
    refresh: once
    on_error: fail
    required: true

transformations:
  - include: "${source.transforms}"
