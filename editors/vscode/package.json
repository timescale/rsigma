{
  "name": "rsigma",
  "displayName": "rsigma — Sigma Detection Rules",
  "description": "Language support for Sigma detection rules: diagnostics, completions, hover, and document symbols powered by rsigma-lsp.",
  "version": "0.1.0",
  "publisher": "rsigma",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/rsigma/rsigma"
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages",
    "Linters"
  ],
  "keywords": [
    "sigma",
    "siem",
    "detection",
    "security",
    "yaml",
    "lsp"
  ],
  "activationEvents": [
    "onLanguage:yaml",
    "workspaceContains:**/*.yml",
    "workspaceContains:**/*.yaml"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "rsigma",
      "properties": {
        "rsigma.serverPath": {
          "type": "string",
          "default": "rsigma-lsp",
          "description": "Path to the rsigma-lsp binary. If not an absolute path, it will be looked up in $PATH."
        },
        "rsigma.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Trace communication between VS Code and the rsigma language server."
        }
      }
    }
  },
  "scripts": {
    "compile": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node --sourcemap",
    "watch": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node --sourcemap --watch",
    "package": "npm run compile && vsce package",
    "lint": "eslint src --ext ts"
  },
  "dependencies": {
    "vscode-languageclient": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/vscode": "^1.120.0",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "vscode-languageclient": {
      "minimatch": "^10.2.1"
    },
    "@vscode/vsce": {
      "undici": "^7.24.0"
    },
    "@azure/msal-node": "^5.2.2",
    "lodash": "^4.17.21",
    "brace-expansion": "^5.0.6"
  }
}
